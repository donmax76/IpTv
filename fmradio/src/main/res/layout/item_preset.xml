<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="48dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="8dp"
    android:layout_marginBottom="2dp"
    android:layout_marginStart="2dp"
    android:layout_marginEnd="2dp"
    android:background="@drawable/bg_preset_button">

    <TextView
        android:id="@+id/tvPresetIndex"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:textColor="@color/lcd_dim"
        android:textSize="12sp"
        android:fontFamily="monospace"
        android:gravity="center" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="6dp">

        <TextView
            android:id="@+id/tvPresetFrequency"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/lcd_green"
            android:textSize="16sp"
            android:textStyle="bold"
            android:fontFamily="monospace" />

        <TextView
            android:id="@+id/tvPresetName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/lcd_amber"
            android:textSize="12sp"
            android:fontFamily="monospace"
            android:singleLine="true"
            android:visibility="gone" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/btnPresetDelete"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="Delete preset"
        android:scaleType="centerInside"
        android:padding="8dp"
        android:tint="@color/lcd_dim" />

</LinearLayout>
