# 🎙️ Аудио Рекордер

Программа для записи звука с микрофона компьютера с графическим интерфейсом на C#.

## 📋 Описание

Это приложение Windows Forms позволяет:
- Записывать аудио с микрофона в формате WAV
- Воспроизводить записанные файлы
- Сохранять записи с автоматической датой и временем
- Открывать папку с записями
- **Выбирать устройство записи** (включая виртуальные драйверы WASAPI)

## 🔧 Требования

- .NET 6.0 или выше
- Windows OS
- Микрофон

## 📦 Установка и запуск

### Метод 1: Запуск из исходного кода

1. Убедитесь, что у вас установлен [.NET 6.0 SDK](https://dotnet.microsoft.com/download/dotnet/6.0) или выше

2. Откройте командную строку в папке с проектом

3. Восстановите зависимости:
```bash
dotnet restore
```

4. Запустите приложение:
```bash
dotnet run
```

### Метод 2: Сборка исполняемого файла

1. Соберите проект:
```bash
dotnet build -c Release
```

2. Запустите файл из папки `bin\Release\net6.0-windows\`:
```bash
.\AudioRecorder.exe
```

### Метод 3: Создание автономного приложения

Создать исполняемый файл, который не требует установки .NET:

```bash
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

Исполняемый файл будет в папке `bin\Release\net6.0-windows\win-x64\publish\`

## 🎯 Как использовать

1. **Запустите приложение** - откроется окно с интерфейсом записи

2. **Начните запись** - выберите нужный микрофон (включая виртуальное устройство из драйвера) и нажмите кнопку "▶ Начать запись"
   - Приложение автоматически получит доступ к выбранному микрофону
   - Статус изменится на "Запись идет..."
   - Таймер покажет длительность записи

3. **Остановите запись** - нажмите кнопку "⬛ Остановить запись"
   - Запись автоматически сохранится в формате WAV
   - Появится окно с путем к сохраненному файлу

4. **Воспроизведите запись** - нажмите кнопку "🔊 Воспроизвести запись"
   - Последняя записанная аудиозапись будет воспроизведена

5. **Откройте папку с записями** - нажмите кнопку "📁 Открыть папку"
   - Откроется папка с сохраненными записями

## 📁 Место сохранения записей

Все записи автоматически сохраняются в папке:
```
C:\Users\[ВашеИмя]\Documents\AudioRecordings\
```

Формат имени файла: `Recording_YYYY-MM-DD_HH-mm-ss.wav`

Например: `Recording_2025-11-07_14-30-45.wav`

## 🎵 Параметры аудио

- **Формат**: WAV (несжатый)
- **Частота дискретизации**: 44100 Hz (CD качество)
- **Каналы**: Моно (1 канал)
- **Битрейт**: 16 бит

## 🔨 Технологии

- **C# / .NET 6.0** - основной фреймворк
- **Windows Forms** - графический интерфейс
- **NAudio 2.2.1** - библиотека для работы с аудио

## 📝 Структура проекта

```
TestApp/
├── AudioRecorder.csproj      # Файл проекта
├── Program.cs                 # Точка входа приложения
├── MainForm.cs                # Главная форма с интерфейсом
├── AudioRecorderService.cs   # Сервис для записи аудио
└── README.md                  # Документация
```

## ⚠️ Устранение неполадок

### Микрофон не обнаружен
- Убедитесь, что микрофон подключен и включен
- Проверьте настройки звука Windows
- Дайте приложению разрешение на доступ к микрофону

### Ошибка при воспроизведении
- Убедитесь, что файл не поврежден
- Проверьте, что файл существует в папке записей
- Попробуйте воспроизвести файл в другом плеере

### Ошибка при запуске
- Убедитесь, что установлен .NET 6.0 Runtime
- Проверьте, что все зависимости установлены (`dotnet restore`)

## 📄 Лицензия

Этот проект создан в образовательных целях и может свободно использоваться и модифицироваться.

## 👨‍💻 Автор

Создано с использованием C# и библиотеки NAudio.

---

**Примечание**: Для работы приложения требуется подключенный и работающий микрофон.

