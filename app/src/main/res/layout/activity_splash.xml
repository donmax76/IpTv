<?xml version="1.0" encoding="utf-8"?>
<!-- Round 222: минимальный сплеш. БЕЗ надписи «Проверка обновлений…»
     по жалобе юзера «этого окна быть не должно». Только лого + имя
     приложения. Если апдейта нет — мелькнёт долю секунды и
     запустится MainActivity. Если апдейт есть — показывается
     поверх этого лого AlertDialog. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/background">

    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_playlist"
        android:tint="@color/primary"
        android:layout_marginBottom="20dp"
        android:contentDescription="@string/app_name"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@color/text_primary"
        android:textSize="28sp"
        android:textStyle="bold"/>

</LinearLayout>
