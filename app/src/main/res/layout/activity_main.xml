<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main content (bottom nav + fragment container) -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background">

        <FrameLayout
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <!-- Глобальный баннер уведомления о фоновом обновлении EPG.
             Висит поверх любого экрана пока EpgRepository.isRefreshing.
             Подписку держит MainActivity.onResume / onPause через
             EpgRepository.addRefreshStateListener. -->
        <LinearLayout
            android:id="@+id/epgRefreshBanner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="6dp"
            android:background="#4ECDC4"
            android:elevation="4dp"
            android:visibility="gone">

            <ProgressBar
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginEnd="8dp"
                android:indeterminateTint="@android:color/white"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/epg_refresh_running_banner"
                android:textColor="@android:color/white"
                android:textSize="12sp"/>

        </LinearLayout>

        <!-- Touch-доступная кнопка меню — открывает боковой drawer.
             Не focusable, на TV-боксе работает через DPAD_LEFT.
             На телефоне без неё открыть меню нельзя — swipe от края
             не всегда срабатывает на чехлах и эмуляторах. -->
        <ImageButton
            android:id="@+id/btnTouchMenu"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentStart="true"
            android:layout_margin="8dp"
            android:src="@drawable/ic_menu"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/settings"
            android:focusable="false"
            android:padding="10dp"
            android:tint="@color/text_primary"/>

        <!-- Bottom navigation kept inflated for code references but hidden:
             the user explicitly asked to navigate via the side drawer only. -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="@color/surface"
            app:itemIconTint="@color/bottom_nav_color"
            app:itemTextColor="@color/bottom_nav_color"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_navigation"
            app:itemRippleColor="@color/primary"
            android:focusable="false"
            android:visibility="gone"/>
    </RelativeLayout>

    <!-- Slide-out side rail (full 6-item nav with Recent and Settings) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/sideNav"
        android:layout_width="260dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/surface"
        app:itemTextColor="@color/text_primary"
        app:itemIconTint="@color/text_primary"
        app:itemTextAppearance="@style/SideNavTextAppearance"
        app:menu="@menu/side_navigation"/>

</androidx.drawerlayout.widget.DrawerLayout>
