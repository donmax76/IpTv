<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:descendantFocusability="beforeDescendants">

    <!-- Тонкий статус-бар: видим когда идёт фоновое обновление EPG.
         Юзер выходит из Settings → плейлист продолжает работать +
         тут показывается "Обновляю ТВ Гид…" пока fetchAll бежит.
         Когда обновление завершилось, бар скрывается автоматически. -->
    <TextView
        android:id="@+id/epgRefreshIndicator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="6dp"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="@android:color/white"
        android:background="#4ECDC4"
        android:text="@string/epg_refresh_running_banner"
        android:visibility="gone"/>

    <!-- Header with playlist name -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/section_padding">

        <TextView
            android:id="@+id/playlistTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/channels"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/title_text"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/channelCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/subtitle_text"
            android:layout_marginTop="2dp"/>

    </LinearLayout>

    <!-- Search bar -->
    <EditText
        android:id="@+id/searchEditText"
        android:layout_width="match_parent"
        android:layout_height="@dimen/search_height"
        android:layout_marginStart="@dimen/search_margin_h"
        android:layout_marginEnd="@dimen/search_margin_h"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_search"
        android:drawableStart="@drawable/ic_search"
        android:drawablePadding="8dp"
        android:drawableTint="@color/text_hint"
        android:hint="@string/search_channels"
        android:textColorHint="@color/text_hint"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/search_text"
        android:inputType="text"
        android:imeOptions="actionSearch"
        android:singleLine="true"
        android:focusable="true"/>

    <!-- HD/4K filter chips -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:layout_marginBottom="6dp"
        android:paddingStart="@dimen/section_padding"
        android:paddingEnd="@dimen/section_padding">

        <LinearLayout
            android:id="@+id/qualityChipsContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"/>

    </HorizontalScrollView>

    <!-- Category chips -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/categoriesRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:clipToPadding="false"
        android:paddingStart="@dimen/section_padding"
        android:paddingEnd="@dimen/section_padding"
        android:focusable="false"/>

    <!-- Channel list -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipeRefresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/channelsRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="8dp"
                android:focusable="true"
                android:descendantFocusability="afterDescendants"
                android:nextFocusDown="@+id/channelsRecyclerView"/>

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:visibility="gone"
            android:indeterminateTint="@color/primary"/>

        <LinearLayout
            android:id="@+id/emptyLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">

            <TextView
                android:id="@+id/emptyText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/select_playlist_first"
                android:textColor="@color/text_hint"
                android:textSize="@dimen/settings_title_text"/>

        </LinearLayout>

    </FrameLayout>

</LinearLayout>
