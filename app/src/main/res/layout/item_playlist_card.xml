<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardBackgroundColor="@color/card_background"
    app:cardCornerRadius="@dimen/playlist_card_radius"
    app:cardElevation="4dp"
    app:strokeWidth="0dp"
    android:focusable="true"
    android:focusableInTouchMode="false"
    android:foreground="@drawable/item_playlist_focused">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/playlist_card_padding"
        android:minHeight="80dp">

        <ImageView
            android:id="@+id/playlistIcon"
            android:layout_width="@dimen/playlist_icon_size"
            android:layout_height="@dimen/playlist_icon_size"
            android:src="@drawable/ic_playlist"
            android:tint="@color/primary"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/playlistIcon"
            android:layout_toStartOf="@+id/btnEdit"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/playlistName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/playlist_name_text"
                android:textStyle="bold"
                android:singleLine="true"
                android:ellipsize="end"/>

            <TextView
                android:id="@+id/playlistUrl"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/playlist_url_text"
                android:singleLine="true"
                android:ellipsize="end"
                android:layout_marginTop="2dp"/>

            <TextView
                android:id="@+id/playlistChannelCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/primary_light"
                android:textSize="@dimen/playlist_url_text"
                android:layout_marginTop="4dp"
                android:visibility="gone"/>

        </LinearLayout>

        <!-- Android Round 372: две отдельные видимые кнопки —
             «Редактировать» (карандаш) и «Удалить» (корзина). Долгое
             нажатие полностью убрано (давало двойное открытие). -->
        <ImageButton
            android:id="@+id/btnDelete"
            android:layout_width="@dimen/favorite_btn_size"
            android:layout_height="@dimen/favorite_btn_size"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_delete"
            android:background="@drawable/item_playlist_focused"
            android:contentDescription="@string/playlist_delete"
            android:visibility="visible"
            android:focusable="true"
            android:focusableInTouchMode="false"/>

        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="@dimen/favorite_btn_size"
            android:layout_height="@dimen/favorite_btn_size"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/btnDelete"
            android:layout_marginEnd="4dp"
            android:src="@drawable/ic_edit"
            android:background="@drawable/item_playlist_focused"
            android:contentDescription="@string/playlist_edit"
            android:visibility="visible"
            android:focusable="true"
            android:focusableInTouchMode="false"/>

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>
