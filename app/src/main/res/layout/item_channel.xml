<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/channel_card_margin_h"
    android:layout_marginEnd="@dimen/channel_card_margin_h"
    android:layout_marginTop="@dimen/channel_card_margin_v"
    android:layout_marginBottom="@dimen/channel_card_margin_v"
    app:cardBackgroundColor="@color/card_background"
    app:cardCornerRadius="@dimen/channel_card_radius"
    app:cardElevation="2dp"
    app:strokeWidth="0dp"
    android:focusable="true"
    android:focusableInTouchMode="false"
    android:foreground="@drawable/item_channel_focused">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="@dimen/channel_card_padding">

        <TextView
            android:id="@+id/channelNumber"
            android:layout_width="@dimen/channel_number_size"
            android:layout_height="@dimen/channel_number_size"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="@dimen/channel_number_text"
            android:textStyle="bold"
            android:background="@drawable/bg_channel_number"/>

        <ImageView
            android:id="@+id/channelLogo"
            android:layout_width="@dimen/channel_logo_size"
            android:layout_height="@dimen/channel_logo_size"
            android:layout_marginStart="12dp"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            android:src="@drawable/ic_channel_placeholder"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="12dp">

            <TextView
                android:id="@+id/channelName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/channel_name_text"
                android:textStyle="bold"
                android:singleLine="true"
                android:ellipsize="end"/>

            <TextView
                android:id="@+id/channelGroup"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_hint"
                android:textSize="@dimen/channel_epg_text"
                android:singleLine="true"
                android:visibility="gone"/>

            <TextView
                android:id="@+id/channelEpg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/secondary"
                android:textSize="@dimen/channel_epg_text"
                android:singleLine="true"
                android:ellipsize="end"
                android:layout_marginTop="2dp"
                android:visibility="gone"/>

        </LinearLayout>

        <ImageButton
            android:id="@+id/btnFavorite"
            android:layout_width="@dimen/favorite_btn_size"
            android:layout_height="@dimen/favorite_btn_size"
            android:src="@drawable/ic_favorite"
            android:tint="@color/favorite_inactive"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/favorite"
            android:focusable="true"
            android:focusableInTouchMode="false"/>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
