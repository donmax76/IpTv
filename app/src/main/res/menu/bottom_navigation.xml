<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/nav_playlists"
        android:icon="@drawable/ic_playlist"
        android:title="@string/playlists"/>
    <item
        android:id="@+id/nav_channels"
        android:icon="@drawable/ic_channels"
        android:title="@string/channels"/>
    <!-- ТВ Гид удалён по просьбе пользователя: программа сейчас
         показывается прямо в списке каналов (now-playing бар под
         каждой строкой). Отдельная вкладка дублирует информацию. -->
    <item
        android:id="@+id/nav_favorites"
        android:icon="@drawable/ic_favorite"
        android:title="@string/favorites"/>
    <item
        android:id="@+id/nav_settings"
        android:icon="@drawable/ic_settings"
        android:title="@string/settings"/>
</menu>
