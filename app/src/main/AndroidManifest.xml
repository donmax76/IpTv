<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:name=".TVViewerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/tv_banner"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVViewer"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <activity
            android:name=".CrashReportActivity"
            android:exported="false"
            android:theme="@style/Theme.TVViewer"
            android:noHistory="true" />

        <!-- Round 221: SplashActivity — точка входа. Проверяет апдейт
             ДО показа MainActivity, чтобы юзер сначала видел диалог
             обновления, а потом уже саму программу. -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TVViewer"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TVViewer"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:resizeableActivity="true"/>

        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.TVViewer.Player"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:screenOrientation="sensorLandscape"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".AddPlaylistActivity"
            android:exported="false"
            android:theme="@style/Theme.TVViewer"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.TVViewer.SettingsPanel"
            android:parentActivityName=".MainActivity" />

        <!-- Round 186: FileProvider нужен чтобы отдать content:// URI
             на скачанный APK системному установщику (PackageInstaller на
             Android 7+ не принимает file://). Authority совпадает с тем
             что мы передаём в FileProvider.getUriForFile() в
             UpdateInstaller. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
