# Использование WindhawkMod БЕЗ установки Windhawk

## ✅ Решение: Standalone Loader

Создан **StandaloneLoader.exe**, который загружает `WindhawkMod.dll` напрямую в `explorer.exe` **БЕЗ необходимости установки Windhawk**!

---

## 🚀 Быстрый старт

### Вариант 1: Автоматический (рекомендуется)

```batch
cd WindhawkMod
USE_STANDALONE.bat
```

Скрипт автоматически:
1. Проверит наличие DLL
2. Соберет StandaloneLoader (если нужно)
3. Загрузит DLL в explorer.exe
4. Покажет результат

### Вариант 2: Ручной запуск

```batch
cd WindhawkMod\bin\x64\Release
StandaloneLoader.exe WindhawkMod.dll
```

**Требования:**
- Запуск от имени администратора
- WindhawkMod.dll должен быть в той же папке или указан полный путь

---

## 📋 Как это работает

1. **StandaloneLoader.exe** находит процесс `explorer.exe`
2. Использует DLL injection для загрузки `WindhawkMod.dll`
3. DLL инициализируется и начинает скрывать иконку микрофона
4. **Windhawk НЕ требуется!**

---

## 🔧 Сборка StandaloneLoader

Если StandaloneLoader еще не собран:

```batch
cd WindhawkMod\StandaloneLoader
msbuild StandaloneLoader.vcxproj /p:Configuration=Release /p:Platform=x64
```

Или используйте `USE_STANDALONE.bat` - он соберет автоматически.

---

## ✅ Преимущества

- ✅ **Не требует Windhawk** - полностью автономное решение
- ✅ **Простое использование** - один .bat файл
- ✅ **Без зависимостей** - только DLL и загрузчик
- ✅ **Портативное** - можно переносить между компьютерами

---

## 📁 Структура файлов

```
WindhawkMod/
├── bin\x64\Release\
│   ├── WindhawkMod.dll          ← Мод для скрытия иконки
│   └── StandaloneLoader.exe     ← Загрузчик (не требует Windhawk)
├── StandaloneLoader/            ← Исходный код загрузчика
│   ├── StandaloneLoader.cpp
│   └── StandaloneLoader.vcxproj
└── USE_STANDALONE.bat           ← Автоматический запуск
```

---

## 🧪 Тестирование

1. **Запустите загрузчик:**
   ```batch
   USE_STANDALONE.bat
   ```

2. **Начните запись аудио:**
   ```batch
   cd ..
   dotnet run --project HiddenAudioService\HiddenAudioService.csproj
   ```

3. **Проверьте результат:**
   - Иконка микрофона должна быть скрыта в системном трее
   - Проверьте логи: `%TEMP%\WindhawkMod.log`

---

## 🔍 Отладка

### Если не работает:

1. **Проверьте права администратора:**
   - Запустите командную строку от имени администратора
   - Или правой кнопкой на `USE_STANDALONE.bat` → "Запуск от имени администратора"

2. **Проверьте логи:**
   ```batch
   type %TEMP%\WindhawkMod.log
   ```

3. **Проверьте, что explorer.exe запущен:**
   ```batch
   tasklist | findstr explorer
   ```

4. **Проверьте DLL:**
   ```batch
   dir WindhawkMod\bin\x64\Release\WindhawkMod.dll
   ```

---

## 📝 Примечания

- **Требуется права администратора** для DLL injection
- **Работает только с x64** версией Windows
- **Требуется Windows 10/11**
- После перезапуска explorer.exe нужно запустить загрузчик снова

---

## 🎯 Результат

После запуска `USE_STANDALONE.bat`:
- ✅ WindhawkMod.dll загружен в explorer.exe
- ✅ Иконка микрофона скрыта в системном трее
- ✅ **Windhawk НЕ требуется!**

---

## 💡 Альтернатива: С Windhawk

Если вы все же хотите использовать Windhawk:
1. Скачайте Windhawk portable: https://windhawk.net/
2. Распакуйте в `D:\Android_Projects\TestApp\Windhawk\`
3. Используйте `LOAD_MOD.ps1` для загрузки мода

Но **StandaloneLoader работает без Windhawk!**

---

**Готово к использованию без Windhawk!** 🎉

